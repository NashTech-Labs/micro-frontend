-- This script was generated by the ERD tool in pgAdmin 4.
-- Please log an issue at https://github.com/pgadmin-org/pgadmin4/issues/new/choose if you find any bugs, including reproduction steps.
BEGIN;


CREATE TABLE IF NOT EXISTS public.admin
(
    id serial NOT NULL,
    email character varying COLLATE pg_catalog."default" NOT NULL,
    phone character varying COLLATE pg_catalog."default" NOT NULL,
    password character varying COLLATE pg_catalog."default" NOT NULL,
    status character varying COLLATE pg_catalog."default" NOT NULL,
    role character varying COLLATE pg_catalog."default" NOT NULL DEFAULT 'super admin'::character varying,
    image character varying COLLATE pg_catalog."default",
    created_at timestamp without time zone NOT NULL DEFAULT now(),
    updated_at timestamp without time zone NOT NULL DEFAULT now(),
    CONSTRAINT "PK_e032310bcef831fb83101899b10" PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS public.subscription
(
    id serial NOT NULL,
    "planName" character varying COLLATE pg_catalog."default" NOT NULL,
    "numberOfEmployees" integer NOT NULL,
    "planDuration" double precision NOT NULL,
    "planDescription" character varying COLLATE pg_catalog."default" NOT NULL,
    price double precision NOT NULL,
    status character varying COLLATE pg_catalog."default" NOT NULL,
    created_at timestamp without time zone NOT NULL DEFAULT now(),
    updated_at timestamp without time zone NOT NULL DEFAULT now(),
    CONSTRAINT "PK_8c3e00ebd02103caa1174cd5d9d" PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS public.tenant
(
    id serial NOT NULL,
    tenant_name character varying COLLATE pg_catalog."default" NOT NULL,
    tenant_email character varying COLLATE pg_catalog."default" NOT NULL,
    role character varying COLLATE pg_catalog."default" NOT NULL,
    tenant_code character varying COLLATE pg_catalog."default" NOT NULL,
    password character varying COLLATE pg_catalog."default" NOT NULL,
    status character varying COLLATE pg_catalog."default" NOT NULL,
    phone character varying COLLATE pg_catalog."default",
    location character varying COLLATE pg_catalog."default",
    subscription_details character varying COLLATE pg_catalog."default",
    company_type character varying COLLATE pg_catalog."default",
    "isRegistered" boolean,
    image character varying COLLATE pg_catalog."default",
    created_at timestamp without time zone NOT NULL DEFAULT now(),
    updated_at timestamp without time zone NOT NULL DEFAULT now(),
    CONSTRAINT "PK_da8c6efd67bb301e810e56ac139" PRIMARY KEY (id)
);
END;